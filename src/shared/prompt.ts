// ==========================================
// Prompt 模板类型定义
// ==========================================

/** Prompt 模板 */
export interface PromptTemplate {
  id: string
  name: string
  content: string
  createdAt: string
}